# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  Default

steps:
- script: echo Hello, world!
  displayName: 'Run a one-line script'

- script: |
    echo Add other tasks to build, test, and deploy your project.
    echo See https://aka.ms/yaml
  displayName: 'Run a multi-line script'
- task: UiPathPack@4
  inputs:
    versionType: 'AutoVersion'
    projectJsonPath: '$(Build.SourcesDirectory)\2163-cr1359-enhance-classification-and-extraction\1359-D365UploadVendorInvoice - Connection Working'
    outputPath: '$(Build.ArtifactStagingDirectory)\Output'
- task: UiPathDeploy@4
  inputs:
    orchestratorConnection: 'AzureCICD'
    packagesPath: '$(Build.ArtifactStagingDirectory)'
    folderName: 'Testing'